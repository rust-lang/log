# This is a hack, see comment in the root Cargo.toml
[workspace]

[package]
name = "integration"
version = "0.1.0"
edition = "2021"
publish = false
build = "src/build.rs"

[features]
std = ["log/std"]

[dependencies]
log = { path = ".." }

[dev-dependencies]
rustversion = "1.0"
