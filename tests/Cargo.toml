[package]
name = "integration"
version = "0.1.0"
edition = "2021"
publish = false
build = "src/build.rs"

[features]
std = ["log/std"]

[dependencies.log]
path = ".."

[dev-dependencies.rustversion]
version = "1.0"
